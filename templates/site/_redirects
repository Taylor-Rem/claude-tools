# Cloudflare Pages reads this file (it is not served). One rule per line:
# from  to  status. `site domain` adds the www -> apex rule when a custom
# domain is attached; add others here (an old page that moved, a short link).
